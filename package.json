{
  "name": "arcane-ascension",
  "version": "0.1.0",
  "description": "RPG de navegador focado em um Mago - Arcane Ascension",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^5.4.21"
  }
}
